from setuptools import find_packages, setup

# will check the folder containing constructor file,
# if it is there then it will consider as local package
setup(
    name = 'GeoTechnical Chatbot',
    version= '0.0.0',
    author= 'Prakash',
    author_email= 'prakashph79@gmail.com',
    packages= find_packages(),
    install_requires = [] 

)